header.header
    .header_wrapper
        .header_column 
            a(href=routes.home) 
                i.fas.fa-home  MODLE
        .header_column 
            ul
                if !user.isAuthenticated
                    li
                        a(href=routes.join) 가입
                    li
                        a(href=routes.login) 로그인
                else
                    li
                        .header_column
                            a(href=routes.user(user.id))
                                i.fas.fa-bars
                    li
                        a(href=routes.logout) 로그아웃
            